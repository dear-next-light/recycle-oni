# リサイクルの鬼 / RECYCLE ONI v0.1

「まだ使える。」小鬼と施設を配置し、焼却される前の資源を救い出す、8Waveの物流ローグライト。HTML / CSS / Vanilla JavaScript / Canvas 2D製。外部通信・ゲーム用依存パッケージ・バックエンド・ログインは不要です。Sail Bike Runから独立したプロジェクトです。

## 起動

Node.js 18以上で、このフォルダ内から `node server.mjs` を実行し、ブラウザで `http://127.0.0.1:4173` を開いてください。停止は Ctrl+C。依存パッケージのインストールは不要です。

`dist/` を静的Webホスティングに置けばURLで遊べます。ES Modulesを使うため、HTMLの直接ダブルクリックではなくHTTPサーバーで開いてください。Cloudflare Pagesへ `dist/` を直接アップロードして公開できます。GitHubリポジトリは https://github.com/dear-next-light/recycle-oni です。

## 操作

- 「現場に入る」で準備開始。「Wave 開始」で収集車が動きます。
- 小鬼は自動で回収・運搬。施設が処理して利益になります。
- 準備中、下の施設ボタンを押して空きマスをタップすると購入・配置します。資金不足や入口を塞ぐ配置はできません。
- 施設をタップして「施設を移動」、移動先をタップ。移動は無料です。同じ画面からレベルアップできます。
- 小鬼をタップすると得意・特性・体力が見えます。準備中は担当施設を選び直せます。重なった小鬼は担当を変えると別施設へ移るので、残る小鬼を選べます。
- 下部の操作列は横スクロールできます。小鬼追加は最大12体、施設は最大10棟。土地拡張は下の3列を開放します。
- 各Wave後の3択から1個無料で獲得し、次の回収ルートに備えます。最終Wave後は結果へ進みます。
- 「まだ使える！」は1Wave1回、6秒の移動・作業加速。
- 「音 OFF / ON」でミュート切り替え。初期状態はミュートです。
- スマホは横向き推奨。縦画面は回転案内を表示します。

## 物流とバランス

施設は2×2マスの障害物で、入口は左上の1マス上。小鬼はグリッドの幅優先探索で最短経路を歩きます。近い施設は回収・中間工程の運搬が短くなります。

PET・缶は洗浄→圧縮。スマホは到着時に修理施設があれば修理、なければ分解施設に向かいます。修理ルートの方が高利益です。処理済みの中間品は出力待ちになり、次工程の担当小鬼が運びます。施設内の待機品はWave間も残ります。

得意施設は処理速度+25%。ランダム特性は俊足・タフ・暑さ耐性・几帳面。体力の低い小鬼は減速し、自動休憩します。洗浄＋圧縮、修理＋分解はマンハッタン距離5以下で処理+10%。

回収できず道路を出たごみが焼却され、地球の熱が上昇します。25以上で回復低下、50以上で消耗増加、75以上でさらに消耗増加。熱に応じて猛暑・豪雨・嵐の確率が増えます。熱100で失敗。Wave回収率75%以上で熱を4下げます。熱はゲーム用の指標で、実際の排出量ではありません。

ランは実時間でおよそ9〜10分＋準備時間です。投入は各Wave45秒まで、最後の収集車が退出してから準備へ。HUDが0秒になっても到着済みの車両の処理を待ちます。高密度になると新規投入数を減らし、アクティブなごみ40個・収集車3台を守ります。

利益は処理売上の累計、資金は初期資金＋利益−購入額です。焼却回避率は救出数÷投入数。結果の救出数には最終時点の未処理在庫も含まれます。ランクは利益と回避率・最終熱の両方で決まります。

## ファイル構成

- `dist/index.html`：画面の構造
- `dist/style.css`：HUD、メニュー、レスポンシブ表示
- `dist/config.js`：ごみ・施設・特性・強化・Wave・天候・価格・速度などの調整値
- `dist/engine.js`：状態遷移、経路探索、物流、利益・熱・体力計算
- `dist/render.js`：Canvasの地形・施設・小鬼・車両・短い再利用演出
- `dist/audio.js`：Web Audioによる軽量な1ループBGMとSE（音声ダウンロードなし）
- `dist/app.js`：UI操作、音量設定とベスト記録の保存
- `server.mjs`：ローカル確認用静的サーバー
- `tests/simulation.mjs`：ロジックの回帰検証
- `tests/browser.cjs`：ブラウザの8Wave通し・画面サイズ・保存検証

## バランス変更・素材差し替え

基本値は `dist/config.js` の `C`、`trashTypes`、`facilityTypes`、`traits`、`upgrades`、`waveDefinitions`、`weatherEffects` を編集します。強化の効果計算は `engine.js` の `speed`、`rate`、`heatEffect`、`sale`、`burn` に集約しています。

現在の素材はCanvasの軽量な図形スプライトです。後から `dist/assets/` に画像を置き、`render.js` の各描画ループを読み込み済み画像の `drawImage()` に置き換えてください。施設座標・入口・当たり判定は `engine.js` のまま保ちます。

## 保存

localStorageキーは `recycle-oni-v01`。ベスト利益・回避率・FLOW・ランク・ミュート設定だけ保存。進行中ランは保存しません。保存を拒否するブラウザでもプレイできます。

## 検証

`node tests/simulation.mjs` で再現可能な8Waveラン、配置距離、得意差、熱、強化、敗北、再挑戦を検証します。

ブラウザ検証は開発用にPlaywrightとEdgeが必要です。Playwrightへのパスを環境変数 `PLAYWRIGHT_PATH` に指定して `node tests/browser.cjs` を実行します。ゲーム本体には不要です。テスト時のみレスポンスへ検証用フックを追加し、同じゲームロジックの時間を進めています。製品ファイルにテスト用グローバルAPIはありません。

詳細と限界は `TEST-REPORT.md` を参照してください。

